# Tavern Multi-TTS 第三方扩展改造计划

## 当前进度

- Step 0（基线确认与保护）：已完成，记录见 `docs/step-0-baseline.md`。
- Step 1（建立扩展最小骨架）：仓库实现、静态验证和用户实机确认完成。设置面板可显示名称、版本和启用开关。
- Step 2（迁移纯业务模块）：仓库实现和单元测试完成。
- Step 3（替换酒馆助手依赖）：仓库实现和单元测试完成。实机验收见交付说明。
- 后续步骤未开始。未得到用户明确授权前，不进入 Step 4。

## 1. 背景

现有 Tavern Multi-TTS 以酒馆助手脚本形式运行，已经支持：

- MiniMax 云端 TTS。
- Local-GSVI 本地推理。
- `<say char="角色名">...</say>` 台词解析。
- 多角色音色映射。
- 提示词注入。
- 音频预取、缓存、播放与下载。
- 映射存档和测试语音。

现有源码位于：

`D:\酒馆资料\tavern_helper_combo-latest\src\minimax-tts`

现有发布仓库为：

`https://github.com/dreamdana88/Tavern-Multi-TTS`

旧仓库中的单文件 `index.js` 是构建产物，不应作为新项目的源码基础。迁移应以本机 TypeScript/Vue 源码为准。

新项目目标目录：

`D:\git项目\Multi-TTS`

## 2. 改造目标

将 Tavern Multi-TTS 从依赖酒馆助手运行时的脚本，迁移为可通过 Git 仓库地址安装、启停和更新的 SillyTavern 第三方前端扩展。

目标包括：

1. 保留现有 MiniMax 与 Local-GSVI 功能和用户配置能力。
2. 新增 IndexTTS-2.5 本地引擎。
3. 保留并增强多角色音色映射。
4. IndexTTS 采用“角色绑定音色预设，台词动态携带情绪”的设计。
5. 将大文件按业务边界拆分，降低修改和排错成本。
6. 建立可重复的构建、静态检查和测试流程。
7. 迁移期间保留旧酒馆助手脚本作为稳定回退版本。

## 3. 非目标

第一阶段不处理以下内容：

- 不修改 IndexTTS、GPT-SoVITS 或 SillyTavern 本体源码。
- 不把 Python 模型环境打包进扩展。
- 不开发必须安装的 SillyTavern 服务端插件。
- 不承诺真正的流式音频；首版仍以完整 WAV/音频结果播放为准。
- 不在功能迁移完成前删除或停用旧酒馆助手脚本。
- 不在未经确认时提交、推送、发布 Release 或替换实际安装版本。

## 4. 总体设计

### 4.1 运行链路

```text
SillyTavern 消息事件
  -> 解析 <say> 台词
  -> 根据 char 查找角色映射
  -> 选择 TTS 引擎与角色音色
  -> 解析 emotion / intensity
  -> 请求对应 TTS 服务
  -> 写入浏览器音频缓存
  -> 在消息中提供播放与下载控件
```

### 4.2 引擎统一接口

每个引擎实现统一能力契约，避免 UI、消息渲染器直接判断不同接口细节。

建议的概念接口：

```ts
interface TtsEngineAdapter {
  readonly id: string;
  checkHealth(): Promise<EngineHealth>;
  listVoices(): Promise<VoiceDescriptor[]>;
  synthesize(request: SynthesisRequest): Promise<Blob>;
}
```

实际字段必须以现有代码和目标服务的真实接口为准，实施时不得凭计划文本臆造。

### 4.3 目录原则

- 按业务能力组织文件，不建立项目级 `components/`、`services/`、`utils/` 杂物箱。
- 引擎内部实现由引擎入口文件封装，外层不得跨级引用内部文件。
- 公共类型和逻辑放在多个直接消费者的最近公共目录。
- 依赖保持单向，禁止循环引用。
- 文件和目录使用 kebab-case；SillyTavern 规定的入口文件名除外。

建议初始结构如下，实施时可依据真实代码调整：

```text
Multi-TTS/
├─ manifest.json
├─ package.json
├─ README.md
├─ AGENTS.md
├─ Tavern-Multi-TTS第三方扩展改造计划.md
├─ src/
│  ├─ extension.ts
│  ├─ extension-settings.ts
│  ├─ engines.ts
│  ├─ engines/
│  │  ├─ minimax.ts
│  │  ├─ local-gsvi.ts
│  │  └─ index-tts.ts
│  ├─ message-tts.ts
│  ├─ message-tts/
│  │  ├─ say-parser.ts
│  │  ├─ message-decoration.ts
│  │  └─ prefetch-queue.ts
│  ├─ audio-cache.ts
│  ├─ audio-playback.ts
│  ├─ prompt-injection.ts
│  ├─ settings-panel.vue
│  └─ style.css
└─ tests/
```

## 5. 多角色与情感设计

### 5.1 角色映射

每个酒馆角色可以绑定：

- 引擎类型。
- 引擎音色标识。
- IndexTTS 音色预设。
- 默认语言。
- 可选的角色级生成参数覆盖。

未命中角色映射时使用引擎默认配置；没有安全默认值时应明确报错，不能静默选择错误音色。

### 5.2 IndexTTS 音色与情绪分离

IndexTTS 角色预设只负责稳定信息：

- 音色参考音频。
- 可选的情绪参考音频。
- 基础生成参数。

每句台词的情绪由标签动态决定，不为每种情绪复制一套角色预设。

建议标签：

```xml
<say char="真田幸影" emotion="restrained-angry" intensity="0.65">出去。</say>
```

职责分离：

```text
char       -> 选择角色音色预设
emotion    -> 选择本地情绪模板
intensity  -> 缩放情绪强度
```

### 5.3 情绪向量

插件将有限、可校验的情绪名称转换为 IndexTTS 八维向量：

```text
喜、怒、哀、惧、厌恶、低落、惊喜、平静
```

第一版要求：

- AI 不直接输出八个数字。
- `emotion` 只能来自允许列表。
- `intensity` 限制在 `0.0` 至 `1.0`。
- 缺少情绪时使用可配置的回退策略：跟随音色参考或 neutral。
- 未知情绪不得直接透传给推理服务，应回退并记录警告。
- 情绪模板允许用户在设置界面调整，但必须经过数值校验和归一化。

第一版暂不支持无限组合情绪。需要复合情绪时，优先增加经过测试的命名模板，如 `restrained-angry`，而不是开放任意数组。

## 6. 分阶段实施计划

### Step 0：基线确认与保护

工作内容：

1. 完整阅读本仓库 `AGENTS.md` 和适用规则。
2. 记录新仓库 Git 状态、分支和 HEAD。
3. 盘点旧脚本源码、构建产物和本地安装版本。
4. 记录旧脚本关键文件 SHA-256。
5. 形成现有功能清单和设置字段清单。
6. 确认目标 SillyTavern 版本和实际安装路径。

验收：

- 明确哪些文件属于旧源码、构建产物和实际安装版本。
- 明确本次允许修改的目录。
- 可以在不影响旧版本的前提下开始迁移。

完成记录：`docs/step-0-baseline.md`（2026-08-13）。

### Step 1：建立扩展最小骨架

工作内容：

1. 创建 `manifest.json`、扩展入口、样式入口和构建配置。
2. 使用 SillyTavern 官方第三方扩展生命周期初始化。
3. 创建最小设置面板，仅显示扩展名称、启用开关和版本。
4. 建立开发安装或符号链接流程，但不得覆盖旧脚本。

验收：

- SillyTavern 可以识别、启用和禁用扩展。
- 扩展卸载或禁用后不残留事件监听和 DOM。
- 控制台无初始化异常。

完成记录：2026-08-13。仓库实现和静态验证完成；用户已在本机 SillyTavern 1.18.0 确认设置面板可显示。

### Step 2：迁移纯业务模块

优先迁移不依赖酒馆助手接口的代码：

1. `<say>` 解析器。
2. 语气词清理。
3. IndexedDB 音频缓存。
4. 播放与下载。
5. 预取队列。
6. MiniMax 和 Local-GSVI 网络请求逻辑。

要求：

- 迁移前后输出保持一致。
- 关键纯函数补充单元测试。
- 网络请求加入超时、可读错误和安全日志，不记录密钥。

验收：

- 单元测试覆盖正常输入、缺失字段、非法标签和网络错误。
- MiniMax/GSVI 适配器可在独立测试中构造正确请求。

完成记录：2026-08-13。已迁移解析器、语气词、新命名空间缓存、播放下载、预取队列和两个引擎适配器。网络测试覆盖超时、取消、非 2xx、非法 JSON 和缺音频字段。未接线到消息渲染。

### Step 3：替换酒馆助手依赖

工作内容：

1. 脚本变量迁移为 SillyTavern 扩展设置。
2. 酒馆助手消息事件迁移为 SillyTavern 原生事件。
3. 消息 DOM 定位迁移为经目标版本验证的原生方式。
4. 提示词注入迁移为 SillyTavern 官方支持的机制。
5. 建立旧配置到新配置的一次性导入工具或手工导入说明。

验收：

- 接收新消息、编辑消息、切换聊天时行为正确。
- 不重复装饰同一段台词。
- 切换或卸载扩展后监听器完整清理。
- 构建通过不等于宿主实测通过；必须在真实 SillyTavern 页面验证。

完成记录：2026-08-13。设置迁到 `extensionSettings.tavern_multi_tts`（schemaVersion 2）。消息监听使用 SillyTavern 1.18.0 的 `MESSAGE_RECEIVED` / `CHARACTER_MESSAGE_RENDERED` / `MESSAGE_UPDATED` / `CHAT_CHANGED`。DOM 定位为 `#chat .mes[mesid] .mes_text`。提示词注入使用 `setExtensionPrompt`（IN_CHAT=1）。提供旧设置 JSON 导入。未开展 Step 4 完整引擎界面。

### Step 4：恢复 MiniMax 与 Local-GSVI 功能

工作内容：

1. 恢复引擎切换。
2. 恢复角色映射和映射存档。
3. 恢复测试语音、预取策略、缓存统计和下载。
4. 对照旧脚本逐项回归。

验收：

- 旧功能清单逐项通过。
- 未切换到新扩展的用户仍可继续使用旧脚本。
- 同时启用旧脚本和新扩展时给出冲突提示，避免重复生成。

### Step 5：接入 IndexTTS-2.5

工作内容：

1. 以本机实际 IndexTTS WebUI API 为准建立适配器。
2. 实现服务健康检查。
3. 拉取或读取 IndexTTS 音色预设。
4. 实现预设到角色的多角色映射。
5. 实现语言、时长系数和基础生成参数。
6. 将音频结果转为 Blob，复用缓存、播放器和下载逻辑。

验收：

- 服务未启动时错误清楚，不阻断整个酒馆页面。
- 同一回复中的多个角色能按顺序使用不同音色。
- 缓存键包含引擎、音色预设、文本、情绪、语言及影响音频的参数。
- 不因角色切换而重新启动 IndexTTS 模型。

### Step 6：动态情绪向量

工作内容：

1. 扩展 `<say>` 解析器，支持 `emotion` 和 `intensity`。
2. 建立默认情绪模板表。
3. 建立模板编辑、校验和重置界面。
4. 更新提示词注入模板，只教 AI 输出情绪名和强度。
5. 增加未知情绪、越界强度和缺省字段测试。

验收：

- 同一角色连续不同情绪台词可生成不同表达。
- AI 无需输出原始八维数组。
- 非法情绪输入不会导致请求崩溃或污染配置。

### Step 7：设置界面与可维护性收口

工作内容：

1. 设置面板按“通用、角色映射、引擎、高级、诊断”分区。
2. 将超大 Vue 文件拆为有明确业务所有权的子树。
3. 增加服务状态、最近错误和请求耗时显示。
4. 增加配置导出和导入。

验收：

- 常用设置无需展开高级选项即可完成。
- 单文件职责清楚，跨目录引用遵守封装边界。
- 手机和桌面均可完成基础配置和播放。

### Step 8：发布准备

工作内容：

1. 完善 README、安装、升级、卸载和故障排查说明。
2. 明确最低 SillyTavern 版本。
3. 提供旧酒馆助手脚本迁移指南。
4. 完成生产构建和干净安装测试。
5. 经用户确认后再提交、推送和发布版本。

验收：

- 使用全新 SillyTavern 用户目录可以按 README 独立安装。
- 更新扩展不会清空用户映射。
- 卸载不会删除外部 TTS 模型和参考音频。

## 7. 测试矩阵

### 静态和单元测试

- TypeScript 类型检查。
- ESLint。
- Prettier 检查。
- `<say>` 解析器测试。
- 情绪模板转换测试。
- 缓存键稳定性测试。
- 各引擎请求构造测试。
- 超时、取消和错误解析测试。

### 浏览器和宿主测试

- SillyTavern 启动、启停扩展、切换聊天。
- 单角色、同回复多角色、未映射角色。
- 消息编辑、重生成、滑动回复和历史消息。
- 手动点击、预取前 N 条、全部预取。
- 播放、暂停、重播、下载和清缓存。
- 桌面与移动端布局。

### 引擎测试

- MiniMax：成功、鉴权失败、限流、超时。
- Local-GSVI：模型目录拉取、角色映射、服务离线。
- IndexTTS：服务检测、预设列表、多角色映射、八维情绪、排队和超时。

## 8. 安全与隐私

- API Key 不写入日志、URL、错误详情或仓库。
- 本地服务地址只按用户配置访问。
- 不自动上传参考音频到第三方服务。
- 第三方响应视为不可信输入，必须校验结构和大小。
- 下载文件名必须清理非法字符。
- 不允许远程内容改变扩展配置、删除本地文件或执行代码。

## 9. 回退方案

1. 旧酒馆助手脚本保持原样并保留可导入构建产物。
2. 新扩展使用独立设置命名空间和缓存版本。
3. 每个阶段形成独立、可审阅的变更，不混入无关项目修改。
4. 实机验证失败时停用新扩展并恢复旧脚本，不修改外部 TTS 环境。
5. 不使用破坏性 Git 操作回退用户已有变更。

## 10. 开工门槛

实施任何阶段前必须先完成：

1. 报告当前基线、差距、目标文件和拟修改范围。
2. 确认目标 SillyTavern 安装路径与版本。
3. 确认本阶段验收标准。
4. 确认不会覆盖用户正在使用的旧脚本。
5. 未得到用户明确授权，不进入下一阶段。
