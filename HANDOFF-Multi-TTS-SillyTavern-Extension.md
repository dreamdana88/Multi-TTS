# Multi-TTS 酒馆第三方扩展改造独立窗口 HANDOFF

更新时间：2026-08-15（Asia/Shanghai）

> 给完全没有上下文的新会话：本窗口只负责 `D:\git项目\Multi-TTS` 这个 SillyTavern 第三方前端扩展。IndexTTS 本体的部署、性能和 API 调查由另一个窗口负责；这里不得擅自修改 `D:\AI\IndexTTS-2.5`、GPT-SoVITS 或 SillyTavern 本体。

## 1. 我们在做什么

原项目是用户写的 Tavern Helper/酒馆助手单文件 Multi-TTS 脚本，GitHub 地址曾为：

```text
https://github.com/dreamdana88/Tavern-Multi-TTS
```

现在正在把它改造成正式的 SillyTavern 第三方前端扩展：

```text
https://github.com/dreamdana88/Multi-TTS
```

本地仓库：

```text
D:\git项目\Multi-TTS
```

目标：可以通过 SillyTavern 的“安装扩展”使用 Git 仓库地址安装、启停和更新；支持多引擎、多角色映射、消息内播放/下载、预取与缓存，并最终接入 IndexTTS-2.5。

当前版本：`0.1.0`。

最低目标 SillyTavern：`1.18.0`。

## 2. 不可更改的产品决定

- 这是 SillyTavern 第三方**前端扩展**，不是酒馆助手脚本，也不强制开发服务端插件。
- 不导入、不兼容旧酒馆助手脚本 JSON 配置；新旧数据互不覆盖。
- 新扩展设置使用 `extensionSettings.tavern_multi_tts`，当前 `schemaVersion: 2`。
- 新扩展内部未来升级仍需保护自己已经保存的映射；“不迁移旧脚本”不等于以后可以清空新扩展数据。
- MiniMax、Local-GSVI、IndexTTS 必须走统一引擎适配器边界。
- `char` 非空但没有命中当前引擎映射时，静默跳过，不生成音频，也不回退默认音色。
- 默认音色/模型只用于没有 `char` 的台词和测试语音。
- `<say char="角色名">` 与 `<say char=“角色名”>` 两种引号都要识别。
- 每个 Swipe 的 UI 状态要正确恢复；切回已经生成的 Swipe 应复用缓存。
- 历史消息加载后也必须重新装饰，但不能因此自动批量烧额度。
- IndexTTS 角色音色和动态情绪分离：角色绑定固定参考/预设，每句台词用情绪名与强度驱动本地八维向量。
- AI 默认只输出受控情绪名和 `0.0–1.0` 强度，不直接输出八维数组。

## 3. 当前 Git 与验证基线

做 handoff 时：

```text
HEAD        b2e66a6 第三轮
branch      main
remote      origin/main 与 main 同步
worktree    clean
```

最终独立复跑：

```text
pnpm verify
22 个测试文件
115 / 115 测试通过
typecheck 通过
node typecheck 通过
ESLint 通过
Prettier 通过
Vite build 通过
dist exports 通过
```

这只证明仓库静态和模拟测试通过，不等于真实 SillyTavern、MiniMax 或 GSVI 已验收。

## 4. 已经完成了什么

### Step 0：基线与规则

- 已建立仓库级 `AGENTS.md`，其中包含作用域、接口、架构、TTS、SillyTavern、UI、安全、验证和 Git 规则，以及用户给出的“八荣八耻”。
- 项目计划在 `Tavern-Multi-TTS第三方扩展改造计划.md`。
- 新会话开工前必须完整阅读 `AGENTS.md`，先报告能力、缺口、拟改文件与验证方法。

### Step 1：第三方扩展骨架

- 官方扩展生命周期和设置面板骨架已完成。
- 用户曾在本机 SillyTavern 1.18.0 确认设置面板可以显示。
- 仓库包含 `manifest.json`、构建入口和 `dist` 产物。

### Step 2：旧能力模块化迁移

- `<say>` 解析、语气词处理、播放、下载、IndexedDB 缓存和预取队列已经模块化。
- MiniMax 与 Local-GSVI 已实现统一适配器。
- 网络模拟测试覆盖超时、取消、非 2xx、非法 JSON 和缺音频。
- 缓存使用新命名空间，不覆盖旧脚本缓存。

### Step 3：脱离酒馆助手运行时

- 设置迁入 SillyTavern 官方扩展设置。
- 使用原生消息事件和 `setExtensionPrompt`。
- 当前已处理新消息、历史加载、编辑/更新、连续 Swipe、切换聊天和重复事件。
- 消息 DOM 当前定位依赖 `#chat .mes[mesid] .mes_text`，属于需要真实版本验证的边界。
- 旧脚本 JSON 导入已经明确删除，不要重新加兼容层。

### Step 4：恢复 MiniMax 与 Local-GSVI 完整功能

设置面板已经具备：

- MiniMax / Local-GSVI 引擎切换；
- MiniMax Key、Group ID、区域、默认音色、目录拉取/筛选、模型、语速、音量；
- GSVI 地址、模型目录、语种、情绪、默认模型、语速及高级生成参数；
- 两套彼此隔离的角色映射和目录状态；
- 映射存档保存、读取、删除；
- 默认音色或单角色试听，中/日/英测试句；
- 预取策略、并发、缓存统计、刷新和清空；
- 提示词注入开关、深度、role 和模板。

经过三轮独立审阅和返修，已经静态确认：

- 未映射 `char` 静默跳过，不回退默认音色。
- 重复角色名由最后一条**完整**映射生效。
- MiniMax 和 GSVI 目录/筛选不串数据。
- GSVI `Batch Size` 已进入缓存键。
- 普通 Key、地址、语速、Batch、试听语言等只保存设置，不拆聊天控件。
- 注入字段只同步提示词；引擎、启用状态和角色映射变化才刷新装饰。
- 总开关关闭会删除 `<say>` 提示词、清装饰、停播放、取消在途合成；开启会恢复注入和装饰但不预取。
- 使用 `AbortSignal` 与请求代次双保险，失效/迟到结果不播放、不把旧控件标错。
- Swipe 会立即取消其他 Swipe 请求、停止旧播放并拆旧装饰；即使新 Swipe 是旁白、未映射角色或没有 `<say>` 也不会残留声音和按钮。
- DOM 的 `swipeid` 暂时落后时会等待重试，不会把旧文本冒充新 Swipe。
- 切回旧 Swipe 可以复用缓存。

## 5. 当前卡在哪里

Step 4 的静态实现已通过，当前卡在**真实 SillyTavern 实机验收**，不是卡在单元测试。

尚未获得真实证据：

- 当前安装的扩展是否就是最新 `b2e66a6/dist`，还是酒馆仍在使用旧缓存/旧安装副本。
- 关闭总开关后，真实 SillyTavern 的 `setExtensionPrompt` 注入是否立即消失。
- 播放中切到旁白/未映射/无标签 Swipe 是否立即静音。
- 改 Key、语速、提示词时，真实聊天按钮是否保持不闪烁、不重建。
- 历史消息、连续 Swipe、编辑、重生成和切聊天在真实 DOM 上是否符合模拟测试。
- Local-GSVI 真实拉模型、试听和消息生成是否成功。
- MiniMax 真实请求是否成功；这会消耗额度，未经用户明确授权不得测试。

不要再为了“可能有问题”继续静态猜改。下一步应该先取得真实宿主证据。

## 6. 下一步计划

### Step 4 实机验收

开始前先确认实际安装方式和路径，检查是否为 Git 安装、副本还是开发符号链接。不要覆盖未知用户改动。

最小验收矩阵：

1. 刷新 SillyTavern，确认加载的是当前版本和当前 `dist`。
2. 打开/关闭扩展与面板总开关，观察提示词和消息控件。
3. 修改 API Key、语速、Batch、提示词模板，确认已有消息按钮不闪烁、不重建。
4. 播放语音时切到正常、旁白、未映射和无 `<say>` 的 Swipe，确认立即停止旧声音。
5. 连续切换多个 Swipe，再切回已生成 Swipe，确认缓存复用。
6. 检查历史消息、编辑、重生成、切换聊天和重复事件。
7. 启动本地 GSVI 后，实测拉模型、映射、试听和消息合成。
8. MiniMax 真实请求必须另行获得用户授权，因为会消耗额度。

通过后，把真实 SillyTavern 版本、安装位置、操作步骤和结果写入交付记录；不要只写“我点过了”。

### Step 5：接入 IndexTTS-2.5

必须等待 IndexTTS 窗口交付真实 Gradio API 契约后再施工。

工作内容：

1. 依据本机运行实例的 API 元数据和真实请求建立适配器，禁止猜 endpoint。
2. 健康检查与清楚的服务离线错误。
3. 读取音色参考/预设，建立多角色映射。
4. 支持语言、时长系数和必要基础参数。
5. 音频转 Blob，复用现有缓存、播放器、下载和取消机制。
6. 缓存键包含引擎、角色音色预设、文本、语言及所有影响结果的参数。
7. 本地服务不安全支持并发时使用有序队列；不要盲目用面板并发值压显存。
8. 角色切换不得重启 IndexTTS 模型。

### Step 6：动态情绪向量

1. `<say>` 扩展为 `emotion` 与 `intensity`。
2. 允许有限、可校验的情绪名称；强度限制为 `0.0–1.0`。
3. 情绪名在本地映射为八维向量。
4. 未知情绪、非法强度和缺省字段必须有明确回退。
5. 角色固定音色与每句动态情绪严格分离。

### Step 7：UI 收口

已经认可的方向：

- 信息架构分为总控、角色、引擎、更多；日常主要操作是“这个角色用哪个声音”。
- 映射使用卡片而不是超宽表格。
- 存档收进菜单。
- 试听在当前映射行显示加载、播放和失败状态。
- 提示词模板进入编辑弹层。
- 桌面分栏、手机单列，继续复用酒馆 `text_pole` / `menu_button` 视觉。

重要修正：服务离线时不能禁用整个映射区。仍应允许查看、手填、编辑和管理存档；只禁用拉目录、远程选择和试听等依赖连接的操作。

UI 不要一次性推翻现有可用界面。优先映射卡片和存档菜单，再逐层收口。

### Step 8：发布准备

- README 安装、升级、卸载和排错。
- 干净用户目录安装测试。
- 验证更新不清空新扩展映射。
- 卸载不能删除外部 TTS 模型或参考音频。
- 只有用户明确授权后才能提交、推送、发布或替换实际安装版本。

## 7. 绝对不要再踩的坑

1. **不要把 `pnpm verify` 说成酒馆实机通过。** 静态、模拟、宿主实测、远程 CI 是四层不同证据。
2. **不要再加旧酒馆助手配置导入。** 用户明确选择新旧不迁移。
3. **不要让未映射角色回退默认音色。** 这会让角色串声；非空 `char` 未命中必须静默跳过。
4. **不要只处理新消息。** Swipe、切回旧 Swipe、历史加载、编辑、重生成和切聊天都必须保留正确装饰。
5. **不要把整个设置深度监听直接连到消息重建。** 普通字段变化只保存；提示词同步和消息装饰刷新必须分开。
6. **不要只停已经播放的音频而忽略在途请求。** 必须 AbortSignal 加代次校验，迟到结果不得复活播放。
7. **不要等 `decorate()` 成功才停止旧 Swipe。** 新 Swipe 可能没有可发声段落；Swipe 事件到达就要先停旧播放。
8. **不要忘记总开关也影响提示词注入。** 关闭时必须删除 prompt，开启时按 `injectEnabled` 决定是否恢复。
9. **不要让 MiniMax 与 GSVI 共用音色目录状态。** 已经拆成两套，IndexTTS 也应有自己的目录状态。
10. **不要让缓存键漏掉真实合成参数。** 曾经漏过 GSVI Batch Size；IndexTTS 接入时逐字段对照请求。
11. **不要猜 SillyTavern 事件名、DOM 或 IndexTTS API。** 以目标版本源码、真实页面和运行实例为准。
12. **不要因服务离线锁死角色映射编辑。** 离线配置是正常需求。
13. **不要同时启用旧脚本和新扩展去装饰同一消息。** 会重复生成；新扩展已有冲突提示。
14. **不要修改外部 TTS 环境或真实安装版本。** 除非用户明确把它纳入当前任务。
15. **不要擅自提交、推送、发布、触发付费 MiniMax 请求。** 每一项都需要明确授权。

## 8. 新会话开场执行规则

新会话开始必须先做 Step 0：

1. 完整读取 `D:\git项目\Multi-TTS\AGENTS.md`。
2. 检查 `git status`、当前提交、分支和实际安装版本。
3. 报告当前能力、已知缺口、拟读取/修改文件和验证方法。
4. 如果任务是审阅，只读审阅，不得顺手改代码。
5. 如果任务是跨架构改造，先给计划和回退边界，等用户确认后施工。
6. 保存用户现有改动；不得使用 `git reset --hard` 或覆盖未知文件。

